{
  "name": "@plainlist/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "eslint . --ext .ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^4.1.11"
  }
}
